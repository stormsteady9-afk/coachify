[build]
  command = "pnpm install && pnpm run build"
  functions = "netlify/functions"
  publish = "dist"

[dev]
  command = "pnpm dev"
  port = 3000

# Route all requests to the Remix server function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true
